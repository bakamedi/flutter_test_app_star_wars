import 'package:flutter/material.dart';

class CharactersFilters extends StatelessWidget {
  const CharactersFilters({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

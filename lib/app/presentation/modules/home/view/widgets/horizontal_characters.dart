import 'package:flutter/material.dart';

class HorizontalCharacters extends StatelessWidget {
  const HorizontalCharacters({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
